//declaração de variaveis
let quantidade = 0;
let total = 0;

//função utilizada para calcular a quantidade
function CalculaPonto(){
    
//promp para entrada de dados
quantidade = parseInt(prompt("digite a quantidades de vezes que foi preciso mandar ele parar de rebolar"));

total = total + quantidades;

//Mostra o resultado usando alert
alert(total);
};